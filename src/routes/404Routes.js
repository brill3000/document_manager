// project import
import NotFound from 'layout/404Layout/NotFound';

// ==============================|| AUTH ROUTING ||============================== //

const NotFoundRoute = {
    path: '*',
    element: <NotFound />
};

export default NotFoundRoute;
